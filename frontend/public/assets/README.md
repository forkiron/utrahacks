# Assets

Organized static assets for the frontend.

| Folder    | Contents                    | Used in code              |
|-----------|-----------------------------|----------------------------|
| `images/`| Photos, videos (png, mov)   | `/assets/images/<file>`    |
| `icons/`  | SVGs (icons, logos)         | `/assets/icons/<file>`    |
| `fonts/`  | Optional local font files   | See `app/assets/fonts.ts`  |

**Font configuration** (where fonts are set up): **`frontend/app/assets/fonts.ts`** — that file is the single source of truth for Geist, Geist Mono, and Playfair.
